
//var server = require('http').createServer(require('./lib/server'))
//    ,io = require('socket.io').listen(server);

require('./lib/server').listen(3000);//(io);

//Server running at port 3000
//server.listen(3000);
